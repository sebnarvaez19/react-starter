# React Starter (Made by ChatGPT)

Scaffold for React + Vite + TypeScript with Prettier + ESLint (pre-configured).

Usage:
1. Scaffold a new project from this template (see instructions).
2. Run `npm install`
3. Run `npm run dev` to start the dev server.
4. `npm run lint` / `npm run format`
